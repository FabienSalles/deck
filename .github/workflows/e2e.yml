name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Build deck package
        run: npm run build

      - name: Pack deck package
        run: npm pack --silent --pack-destination e2e

      - name: Install e2e dependencies
        working-directory: e2e
        run: npm install ./fabiensalles-deck-*.tgz

      - name: Verify the install downloaded no browser
        working-directory: e2e
        run: test ! -e node_modules/puppeteer

      - name: Install Playwright browsers
        working-directory: e2e
        run: npx playwright install chromium --with-deps

      - name: Verify deck-pdf resolves a browser at launch
        working-directory: e2e
        run: node scripts/check-pdf-export.mjs

      - name: Build e2e project
        working-directory: e2e
        run: npm run build

      - name: Run e2e tests
        working-directory: e2e
        run: npm test

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results
          path: e2e/test-results/
          retention-days: 7
